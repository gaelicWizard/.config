#!/bin/bash --fail

#shopt -u checkhash
    # Make sure these stick, for a while at least.
    #shopt -s expand_aliases

hash -p /usr/libexec/PlistBuddy PlistBuddy
hash -p /usr/libexec/path_helper path_helper
hash -p ~/Projects/External/GoogleAppsManager.svn/gam.py gam
